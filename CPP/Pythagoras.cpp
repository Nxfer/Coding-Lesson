#include <iostream>
#include <cmath>

using std::cout;
using std::cin;

int main() {
    double a;
    double b;
    double c;

    cout << "Masukkan nilai A: ";
    cin >> a;

    cout << "Masukkan nilai B: ";
    cin >> b;

    c = sqrt(pow(a, 2) + pow(b, 2));
    cout << "Nilai C adalah : " << c;



}            
